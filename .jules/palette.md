## 2026-06-29 - Missing Focus States on Interactive Icon Buttons
**Learning:** Custom interactive icon components in this app (e.g., HeartButton, NotificationBell, SidebarModal close button) frequently omit focus states despite having hover animations, negatively impacting keyboard accessibility.
**Action:** Ensure that all newly created or modified interactive elements, especially icon-only buttons without default browser styles, include explicit `focus-visible` utility classes (e.g., `focus-visible:outline-2 focus-visible:outline-primary focus-visible:outline-offset-2`).

## 2024-05-24 - Consistent Keyboard Focus Indicators
**Learning:** While checking accessibility on key interactive elements (e.g., custom icon buttons, game card triggers), I found that many had hover states but lacked visible focus indicators for keyboard users. Adding a consistent focus ring pattern greatly improves accessibility without compromising the design.
**Action:** Always apply `focus-visible:outline-2 focus-visible:outline-primary focus-visible:outline-offset-2` (or similar utility classes from the established design system) to all interactive elements (`button`, `a`, `input`, etc.) to ensure keyboard navigability.
## 2026-09-02 - Accessible Tab Patterns with Custom Focus States
**Learning:** When building or updating tab-based navigation using standard `<button>` elements styled to look like tabs (e.g., `WishlistTabs.tsx`), they inherently lack semantic meaning for screen readers. Simply styling them is insufficient for accessibility.
**Action:** Always enhance visually styled tabs with semantic ARIA roles (`role="tablist"` on the container, `role="tab"` on the items). explicitly indicate the active state using `aria-selected={condition}`, hide decorative child icons with `aria-hidden="true"`, and ensure custom `focus-visible` utility classes are applied for robust keyboard navigation. Finally, remember to update UI tests to query `getByRole('tab')` instead of `'button'` to prevent test regressions.
## 2026-09-02 - Conditionally Hidden Text on Interactive Elements
**Learning:** When using utility classes to conditionally hide text inside interactive elements across different viewport sizes (e.g., `max-[600px]:[&>span]:hidden` to hide label text on mobile but keep the icon), the accessible name will be lost for screen reader users at those breakpoints, causing E2E tests and accessibility checks to fail.
**Action:** Always provide an explicit `aria-label` attribute on interactive elements that conditionally hide their visible text. Ensure the `aria-label` matches the visible text it is replacing to maintain consistency and pass `getByRole('button', { name: /.../ })` test queries.
